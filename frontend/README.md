# DMS Frontend (scaffold)

Minimal Vite + React + Tailwind scaffold for the DMS frontend.

Quick start:

```powershell
cd frontend
npm install
npm run dev
```

The `src/components/Dashboard.jsx` is a static placeholder matching the layout.
